import React, { Component } from "react";

export default class EditAudit extends Component {
  render() {
    return (
      <div>
        Take Audit from database, tagged to tenant Edit Audit Update Audit
        Notify Auditor
      </div>
    );
  }
}
